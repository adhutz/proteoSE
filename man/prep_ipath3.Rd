% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{prep_ipath3}
\alias{prep_ipath3}
\title{Prepare iPATH3 annotation tables from proteomics results}
\usage{
prep_ipath3(
  x,
  contrasts = "all",
  pval_or_qval = c("pval", "qval"),
  cut_off = 0.05,
  color_mode = c("binary", "continuous"),
  range_mode = c("local", "total"),
  width_range = c(10, 20),
  clip_fc = NULL,
  color_pal = c("#3B82F6", "#eeeeee", "#EF4444"),
  color_res = 256,
  id_col = "uni_prot_ids",
  id_mode = c("first", "all"),
  collapse_duplicates = c("none", "first", "max_width"),
  diff_suffix = "_diff",
  pval_suffix = "_p.val",
  qval_suffix = "_p.adj",
  open_in_browser = TRUE,
  ipath_url = "https://pathways.embl.de/ipath3.cgi?map=metabolic",
  plot_scalebars = TRUE,
  neutral_color = "#eeeeee",
  copy_to_clipboard = FALSE,
  clipboard_mode = c("combined", "first")
)
}
\arguments{
\item{x}{A \code{SummarizedExperiment} or a \code{data.frame}. If a
\code{SummarizedExperiment} is supplied, it is converted to a wide data frame via
\code{DEP2::get_df_wide()}.}

\item{contrasts}{Character vector of contrast names to use, or \code{"all"} to
use all available contrasts detected from the fold-change columns.}

\item{pval_or_qval}{Which significance column type to use for filtering and
width scaling. One of \code{"pval"} or \code{"qval"}.}

\item{cut_off}{Numeric scalar. Significance threshold applied to the selected
p-value or adjusted p-value column.}

\item{color_mode}{Character scalar. One of \code{"binary"} or \code{"continuous"}.
In binary mode, significant positive and negative fold changes are assigned
fixed high/low colors and non-significant entries are grey. In continuous
mode, only significant entries are colored by fold change and
non-significant entries are grey.}

\item{range_mode}{Character scalar. One of \code{"local"} or \code{"total"}. In local
mode, the color scale is computed separately for each contrast. In total
mode, one global symmetric fold-change range is used across all selected
contrasts.}

\item{width_range}{Numeric vector of length 2 giving the minimum and maximum
iPATH3 line widths.}

\item{clip_fc}{Optional numeric scalar. If supplied, fold changes are clipped
to \verb{[-clip_fc, clip_fc]} for color scaling. Values beyond this range are
shown with the most extreme colors.}

\item{color_pal}{Character vector of colors used for the fold-change
gradient. Typically low, midpoint, and high.}

\item{color_res}{Integer scalar giving the number of colors used to build the
gradient.}

\item{id_col}{Character scalar. Column containing UniProt identifiers.
Default is \code{"uni_prot_ids"}.}

\item{id_mode}{How to handle multiple UniProt IDs within one row. One of:
\itemize{
\item \code{"first"}: keep only the first UniProt ID
\item \code{"all"}: split all semicolon-separated UniProt IDs into separate
iPATH3 rows
}}

\item{collapse_duplicates}{How to handle duplicated \code{ipath3_id} values after
ID expansion. One of:
\itemize{
\item \code{"none"}: keep all rows
\item \code{"first"}: keep the first row per \code{ipath3_id}
\item \code{"max_width"}: keep the row with the largest line width per
\code{ipath3_id}
}}

\item{diff_suffix}{Character scalar giving the suffix for fold-change
columns. Default is \code{"_diff"}.}

\item{pval_suffix}{Character scalar giving the suffix for p-value columns.
Default is \code{"_p.val"}.}

\item{qval_suffix}{Character scalar giving the suffix for adjusted p-value
columns. Default is \code{"_p.adj"}.}

\item{open_in_browser}{Logical; if \code{TRUE}, open iPATH3 in the browser.}

\item{ipath_url}{Character scalar. URL to open when \code{open_in_browser = TRUE}.}

\item{plot_scalebars}{Logical; if \code{TRUE}, print one fold-change scalebar per
contrast.}

\item{neutral_color}{Character scalar used for non-significant entries.}

\item{copy_to_clipboard}{Logical; if \code{TRUE}, copy iPATH3 output to the system
clipboard using \code{clipr::write_clip()}.}

\item{clipboard_mode}{How to copy output when \code{copy_to_clipboard = TRUE}.
One of:
\itemize{
\item \code{"combined"}: combine all selected contrasts into one data frame and
add a \code{contrast} column
\item \code{"first"}: copy only the first contrast
}}
}
\value{
A named list, one element per contrast.
}
\description{
Converts a \code{SummarizedExperiment} or wide result data frame into one or more
iPATH3-ready annotation tables, with line color representing fold change and
line width representing statistical significance.
}
\details{
This function is designed for DEP2-style proteomics output where each
contrast has columns such as \code{"<contrast>_diff"} together with either
\code{"<contrast>_p.val"} or \code{"<contrast>_p.adj"}.

For each selected contrast, the function:
\enumerate{
\item identifies the fold-change and significance columns
\item assigns colors according to \code{color_mode}
\item assigns line widths based on significance
\item extracts UniProt IDs from \code{id_col}
\item optionally collapses duplicated \code{ipath3_id} values
\item creates an iPATH3-ready table with columns \code{ipath3_id}, \code{color},
and \code{width}
}

In \code{id_mode = "first"}, only the first UniProt ID per row is used.
In \code{id_mode = "all"}, all semicolon-separated UniProt IDs are expanded into
separate rows.

If \code{copy_to_clipboard = TRUE} and more than one contrast is selected,
\code{clipboard_mode = "combined"} copies a single combined table with a
\code{contrast} column, which is usually the most practical clipboard format.

The returned object is a named list with one entry per contrast. Each entry
contains:
\itemize{
\item \code{df}: iPATH3-ready annotation data frame
\item \code{scalebar}: data frame describing the fold-change color scale
}
}
\examples{
\dontrun{
res_ipath <- prep_ipath3(
  x = se,
  contrasts = "all",
  pval_or_qval = "pval",
  cut_off = 0.01,
  color_mode = "continuous",
  range_mode = "local",
  width_range = c(10, 20),
  clip_fc = 0.58,
  id_mode = "all",
  collapse_duplicates = "max_width"
)

prep_ipath3(
  x = se,
  contrasts = c("tnfa50_48h_vs_ctrl_48h", "il1b_vs_ctrl"),
  copy_to_clipboard = TRUE,
  clipboard_mode = "combined"
)
}

}
