% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprecated.R
\name{sn_ptm_to_ppe}
\alias{sn_ptm_to_ppe}
\title{Convert Spectronaut PTM Report to PhosphoExperiment (deprecated)}
\usage{
sn_ptm_to_ppe(
  file,
  sep = "_rep_",
  filt = c("reverse", "potential_contaminant"),
  experimental_design = NA,
  output_type = "ppe",
  assay_name = "orig_assay",
  mod_type = "Phospho (STY)",
  site_prob = 0.75,
  use_id = "collapse_key_gene"
)
}
\arguments{
\item{file}{Path to Spectronaut export file (TSV) or a data.frame}

\item{sep}{Separator between condition and replicate in sample labels. Default: "\emph{rep}"}

\item{filt}{Character vector of columns used to filter out contaminants. Default: c("reverse", "potential_contaminant")}

\item{experimental_design}{Optional data.frame with columns: label, sample, condition, replicate}

\item{output_type}{Output type: one of "long_df", "wide_df", "ppe", or "all". Default: "ppe"}

\item{assay_name}{Name of the assay in the final PhosphoExperiment object. Default: "orig_assay"}

\item{mod_type}{Character vector of modification types to retain (e.g., "Phospho (STY)"). Default: "Phospho (STY)"}

\item{site_prob}{numeric vector for modifications probabilities to retain (e.g., 0.75). Default: 0.75}

\item{use_id}{Character vector to choose for your "ID" in all subsequent analysis and plots default: "collapse_key_gene")}
}
\value{
Depending on \code{output_type}, returns:
\itemize{
\item \code{"ppe"}: a PhosphoExperiment object
\item \code{"long_df"}: a long-format cleaned tibble
\item \code{"wide_df"}: a wide-format tibble after pivot
\item \code{"all"}: a named list with all three components
}
}
\description{
\strong{Deprecated.} Use \code{\link{read_ptm_to_ppe}} with
\code{source = "spectronaut"} instead. This older Spectronaut converter is
retained unchanged because it builds a different intermediate schema (notably
the \code{use_id = "collapse_key_gene"} identifier) than the current engine.

This function reads in a long-format Spectronaut PTM report (Moritz style),
processes it, and outputs a PhosphoExperiment object, a long dataframe, a wide dataframe,
or a list of all three depending on \code{output_type}.
}
\keyword{internal}
