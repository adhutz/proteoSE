% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiment-design.R
\name{plan_experiment}
\alias{plan_experiment}
\title{Plan Experimental Design with Flexible Batching}
\usage{
plan_experiment(
  type,
  treatment,
  timepoint,
  replicates,
  batched = FALSE,
  batch_list = NULL,
  batch_size = NULL,
  summarize_batches = TRUE,
  export_csv = TRUE,
  export_xlsx = TRUE,
  csv_name = "experiment_design.csv",
  xlsx_name = "experiment_design.xlsx"
)
}
\arguments{
\item{type}{A character vector of experimental types (e.g. \code{"cell_line1"}, \code{"kidney"}, \code{"organoid"}).}

\item{treatment}{A character vector of treatments (e.g. \code{"control"}, \code{"tnfa"}, \code{"lps"}).}

\item{timepoint}{A character vector of timepoints (e.g. \code{"0h"}, \code{"24h"}, \code{"48h"}).}

\item{replicates}{Integer. Number of replicates (e.g. \code{5}).}

\item{batched}{Logical. Whether to assign rows to batches. e.g. batch1 = reps 1:3, batch2 = reps 4:6 or similar (\code{TRUE}/\code{FALSE}).}

\item{batch_list}{Named list of replicate indices for manual batching, e.g. \code{list(batch1 = 1:3, batch2 = 4:6)}.}

\item{batch_size}{Approximate maximum number of rows per batch for automatic batching (mutually exclusive with \code{batch_list}).}

\item{summarize_batches}{Logical. Print a summary of batches to console. Default is \code{TRUE}.}

\item{export_csv}{Logical. Export design to CSV. Default is \code{TRUE}.}

\item{export_xlsx}{Logical. Export design to XLSX. Default is \code{TRUE}.}

\item{csv_name}{File name for CSV export. Default is \code{"experiment_design.csv"}.}

\item{xlsx_name}{File name for XLSX export. Default is \code{"experiment_design.xlsx"}.}
}
\value{
A \code{data.frame} containing the full design with columns: \code{type}, \code{treatment}, \code{timepoint}, \code{rep}, \code{group}, and optionally \code{batch}.
}
\description{
This function generates a full factorial experimental design from specified conditions
(e.g., cell type, treatment, timepoint, replicates) and supports optional batching.
Replicates are always kept intact across batches. Batch summaries and exports to CSV/XLSX
are available as built-in helpers.
}
\examples{
\dontrun{
# Basic full factorial design without batching
plan_experiment(
  type = c("cell_line1", "kidney"),
  treatment = c("control", "tnfa"),
  timepoint = c("0h", "24h"),
  replicates = 3,
  batched = FALSE
)

# With automatic batching (replicates are grouped together)
plan_experiment(
  type = c("cell_line1", "kidney", "organoid"),
  treatment = c("control", "tnfa", "lps"),
  timepoint = c("0h", "24h", "48h"),
  replicates = 6,
  batched = TRUE,
  batch_size = 50
)

# With manual batching
plan_experiment(
  type = c("cell_line1", "kidney"),
  treatment = c("control", "tnfa"),
  timepoint = c("0h", "24h"),
  replicates = 6,
  batched = TRUE,
  batch_list = list(batch1 = 1:3, batch2 = 4:6)
)

}
}
