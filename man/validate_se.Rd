% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_se}
\alias{validate_se}
\title{Check that a SummarizedExperiment carries what a function needs}
\usage{
validate_se(
  se,
  require_rowdata = character(),
  require_coldata = character(),
  require_assays = character(),
  require_contrast = NULL,
  class = "SummarizedExperiment",
  .caller = NULL
)
}
\arguments{
\item{se}{Object to check.}

\item{require_rowdata}{Character vector of column names that must be in
\code{rowData(se)}.}

\item{require_coldata}{Character vector of column names that must be in
\code{colData(se)}.}

\item{require_assays}{Character vector of names that must be in
\code{assayNames(se)}.}

\item{require_contrast}{Character vector of contrast prefixes (e.g.
\code{"treat_vs_ctrl"}) that must have differential-testing results in
\code{rowData(se)}.}

\item{class}{Class \code{se} must inherit from. Use \code{"PhosphoExperiment"} for the
PhosR-based functions; it inherits from \code{SummarizedExperiment}, so the same
checks apply.}

\item{.caller}{Name of the function to blame in the error message. Internal;
the package's own \code{.assert_se()} fills it in.}
}
\value{
\code{se}, invisibly, so the call composes:
\code{se <- validate_se(se, require_coldata = "condition")}.
}
\description{
Fails early with a message that names the missing column, assay or contrast
\emph{and} lists what the object does have — instead of the
\verb{undefined columns selected} error three frames deep that you get otherwise.
}
\details{
Requirements are additive and all optional; an argument left at its default
checks nothing. Only ask for what the calling function genuinely needs: an
object that is missing \code{replicate} still plots fine, so do not require it.
}
\examples{
se <- SummarizedExperiment::SummarizedExperiment(
  assays  = list(intensity = matrix(1:6, nrow = 3)),
  rowData = S4Vectors::DataFrame(gene_names = c("A", "B", "C")),
  colData = S4Vectors::DataFrame(condition = c("ctrl", "treat"))
)
validate_se(se, require_rowdata = "gene_names", require_coldata = "condition")

# Tells you what is there, not just what is missing:
try(validate_se(se, require_assays = "imputed"))

}
