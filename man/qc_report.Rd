% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc-report.R
\name{qc_report}
\alias{qc_report}
\title{Render a quality-control report to a self-contained HTML file}
\usage{
qc_report(
  se,
  group = "condition",
  batch = NULL,
  assay = 1,
  output = "qc_report.html",
  open = interactive()
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} with a \code{condition} column in \code{colData}.}

\item{group}{\code{colData} column used to colour and annotate samples in the PCA
and correlation panels.}

\item{batch}{Optional second \code{colData} column, annotated alongside \code{group} --
the quickest way to see whether the samples cluster by batch instead of by
condition.}

\item{assay}{Assay to report on, by name or index. Defaults to the first,
which for a proteoSE object is the raw intensity matrix; pass e.g.
\code{"imputed_perseus"} to QC the imputed values instead.}

\item{output}{Path of the HTML file to write.}

\item{open}{Open the finished report in a browser.}
}
\value{
The path of the written report, invisibly.
}
\description{
The standard opening sequence of a proteomics analysis -- identification
counts, missingness pattern, intensity distributions, replicate variability
and sample clustering -- rendered in one call so it can be emailed to
collaborators who do not run R.
}
\details{
Every section degrades rather than fails: a panel whose prerequisite is
missing (no missing values to plot, no batch column, too few features for a
PCA) prints a short note and the report carries on.
}
\examples{
\dontrun{
se <- optimized_spectronaut_to_se(
  system.file("extdata", "example_project_report.tsv", package = "proteoSE")
)
qc_report(se, output = "qc.html")
}

}
