% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{proteoSE_pipeline}
\alias{proteoSE_pipeline}
\alias{print.proteoSE_pipeline}
\title{Build a reproducible analysis pipeline}
\usage{
proteoSE_pipeline(input)

\method{print}{proteoSE_pipeline}(x, ...)
}
\arguments{
\item{input}{The object the pipeline starts from -- typically the
\code{SummarizedExperiment} returned by \code{\link[=se_read_in]{se_read_in()}}. Not validated: whatever
the first step accepts is fair game.}

\item{x}{A \code{proteoSE_pipeline}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{proteoSE_pipeline}.
}
\description{
Records the functions and parameters of an analysis instead of running them,
so that the recipe can be inspected, saved, replayed, and turned into a
methods paragraph. A \code{SummarizedExperiment} already carries the \emph{data} a
pipeline produced; what it does not carry is which functions with which
parameters produced it.
}
\details{
Steps are added with \code{\link[=add_step]{add_step()}} and nothing executes until
\code{\link[=run_pipeline]{run_pipeline()}} is called. The object is a plain list with an S3 class, so
\code{saveRDS()} / \code{readRDS()} round-trips it.
}
\examples{
se <- SummarizedExperiment::SummarizedExperiment(
  assays  = list(intensity = matrix(1:6, nrow = 3)),
  rowData = S4Vectors::DataFrame(gene_names = c("A", "B", "C")),
  colData = S4Vectors::DataFrame(condition = c("ctrl", "treat"))
)

pipe <- proteoSE_pipeline(se) |>
  add_step("filter_perseus", perc_na = 0.33) |>
  add_step("test_diff_limma")
pipe

write_methods(pipe)
}
\seealso{
\code{\link[=add_step]{add_step()}}, \code{\link[=run_pipeline]{run_pipeline()}}, \code{\link[=write_methods]{write_methods()}}
}
